{
  "$schema": "https://developers.cloudflare.com/pages/configuration/schema",
  "version": 1,
  "build": {
    "command": "cd frontend && npm install && npm run build",
    "cwd": "/",
    "destination_dir": "frontend/build"
  }
}
