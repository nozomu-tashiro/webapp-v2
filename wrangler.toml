# Cloudflare Pages Configuration
name = "kaketsuke-form-app"
compatibility_date = "2024-12-13"

[site]
bucket = "./frontend/build"

[build]
command = "cd frontend && npm install && npm run build"
cwd = "./"
watch_dir = "frontend"
